 
    import '../repositories/repositories.dart';

    class GetScanninghomeUseCase {
        final ScanninghomeRepository repository;
      
        GetScanninghomeUseCase({required this.repository});
      
        // Future<User> execute(String userId) async {
        //   return userRepository.getUser(userId);
        // }
      }
      